{
  "name": "auction-hardhat",
  "version": "1.0.0",
  "description": "OnlineAuction smart contract with Hardhat + Viem",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "interact": "hardhat run scripts/interact.js --network localhost",
    "coverage": "cross-env SOLIDITY_COVERAGE=true hardhat coverage"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "cross-env": "^7.0.3",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "viem": "^2.0.0"
  }
}
